upstream mail_app {
    server app:3000;
}

server {
    listen 80;
    listen [::]:80;
    server_name mail.dealercms.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://mail_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 120s;
        client_max_body_size 25m;
    }
}

# После certbot раскомментируй или используй nginx.ssl.conf
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name mail.dealercms.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/mail.dealercms.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mail.dealercms.ru/privkey.pem;
#
#     location / {
#         proxy_pass http://mail_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 120s;
#         client_max_body_size 25m;
#     }
# }
